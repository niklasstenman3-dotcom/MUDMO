[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "mudmo-global-verbs"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["PyYAML>=6.0", "pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
