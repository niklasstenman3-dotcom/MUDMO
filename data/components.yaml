integrity:
  fields: [max, current, fracture_threshold]
movable:
  fields: [mass, friction, anchored]
openable:
  fields: [is_open]
lockable:
  fields: [is_locked, lock_quality]
barrable:
  fields: [is_barred]
container:
  fields: [contents, capacity]
combatant:
  fields: [hp, hp_max, stamina, stamina_max, poise, poise_max, guard, guard_max, speed]
anatomy:
  fields: [limbs, bleed_tolerance, injury_slots]
skills:
  fields: [tool_edge, tool_blunt, tool_point, tool_leverage, athletics, grappling, perception, lockwork]
